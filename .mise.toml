[tools]
rust = "1.92.0"
"cargo:cargo-nextest" = "latest"
"cargo:cargo-insta" = "latest"
"cargo:tree-sitter-cli" = "latest"

[tasks.fmt]
description = "Format the whole workspace"
run = "cargo fmt --all"

[tasks."fmt:check"]
description = "Check formatting"
run = "cargo fmt --all --check"

[tasks.lint]
description = "Clippy with warnings-as-errors"
run = "cargo clippy --workspace --all-targets --all-features -- -D warnings"

[tasks.test]
description = "Run the test suite via nextest"
run = "cargo nextest run --workspace --all-features"

[tasks.cover]
description = "Coverage (requires cargo-llvm-cov installed separately)"
run = "cargo llvm-cov --workspace --all-features"

[tasks.bench]
description = "Run benches"
run = "cargo bench --workspace"

[tasks."grammar:generate"]
description = "Regenerate tree-sitter-lsl parser from grammar.js"
run = "cd crates/tree-sitter-lsl && tree-sitter generate"

[tasks."grammar:test"]
description = "Run tree-sitter-lsl corpus tests"
run = "cd crates/tree-sitter-lsl && tree-sitter test"
